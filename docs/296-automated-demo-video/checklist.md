# 296 — Automated demo video checklist

- [x] Resolve the open question in `requirements.md` (demo-instance host) — req 14, 2026-09-16; instance deployed on `services` (plan §9)
- [x] Create the `shipit-demo-app` repo on the demo GitHub account: Vite + React scaffold, `docker-compose.yml` with the `x-shipit-preview: auto` dev service, `shipit.yaml` with `agent.install: npm ci`, `.claude/settings.json` (`ANTHROPIC_BASE_URL` → `http://demo-proxy:8787`, no key), no `.github/workflows`, no branch protection; record the snapshot SHA — `main` `af344fbe…` (plan §1)
  - [x] Repo created public through the instance's `POST /api/repos` (template `react-vite-ts`): `main` `c837844d…` has the lockfile, the `dev` compose service (5173, `auto` by default), `shipit.yaml`; preview verified rendering on the instance (plan §1)
  - [x] The demo-specific files through one agent turn (redirect, explicit `x-shipit-preview: auto`, `agent.install: npm ci`), merged from the PR card; then re-pin `website-hero/storyboard.json` `repo.commit` — PR #1 from session `4f82fcf3` on the DeepSeek route, merged via `POST /api/sessions/:id/pr/merge`; pin moved to `af344fbe…`, verified in a fresh pull (three files exact, no `.github/`)
- [x] Add `playwright` as an exact-pinned `devDependency` (≥ 7 days old, `npm run check-deps` green) — with user sign-off if a younger release is needed (1.62.1, published 2026-07-30)
- [x] The demo instance itself — `scripts/demo-video/compose.yml` was not written; the local install recipe (`deployment/local/setup.sh` + `tailscale.sh`, `SESSION_EGRESS_ENFORCE=0` via `.shipit.env`) deployed it on `services` at `http://100-81-125-94.sslip.io:4123` (plan §9). State is the `shipit-prod_workspace` volume, not a bind-mounted `SHIPIT_STATE_DIR`; no env-adopted `ANTHROPIC_API_KEY` / `GITHUB_TOKEN` — both entered once by Nik
- [x] `demo-proxy` as a Compose sibling on the instance's `shipit-prod` network (plan §2, §9) — its own project `shipit-demo` joining the network as `external` (`scripts/demo-video/host/`), since `lib.sh` discovers no overlays beyond the fixed tailnet file; up on `services`, `HEAD http://demo-proxy:8787/api/hello` → 200 from a container on the network; `/root/shipit-demo-proxy.env` is an optional key override (`env_file` `required: false`)
- [x] Verify the settings-file redirect works from inside a session container on the demo instance: measured 2026-09-16 on the subscription (account) route — every request of a real turn arrived at `demo-proxy` on the bearer lane, none at the vendor (plan §9). A key-mode credential's *shaped* container spawn is not what the demo runs on and stays unmeasured.
  - [x] …for the *in-process* spawn of the dogfood instance (plan §8): a Z.ai-shaped turn arrived at the proxy on the `x-api-key` lane, nothing reached the vendor
- [x] base-URL-only redirect reaches the proxy with the instance's own credential (first record take, 2026-09-16) — measured on the subscription's **`bearer`** lane, not `x-api-key`: the demo instance's turn route is Nik's Claude subscription (no key file anywhere), and every one of the turn's 25 requests arrived at the proxy as `Authorization: Bearer`, zero on `x-api-key`; the cassette holds no auth header (grep for `authorization` / `x-api-key` over the take is empty). The probe had measured the redirect with a dummy key present
- [x] `proxy.mjs` — lanes by auth header kind; record mode (forward the caller's credential, `DEMO_PROXY_ANTHROPIC_API_KEY` as an optional `x-api-key` override, save `<lane>/<n>.sse` + fingerprints, no request header in the cassette); replay mode (nth response per lane, paced SSE, drift log, `HEAD /api/hello` → 200, exhausted cassette → 400, non-retryable; a chunked request is forwarded with `content-length` and no `transfer-encoding`); `proxy.test.ts`
  - [x] Record mode exercised against the real vendor — 2026-09-16 on the demo instance, on the subscription's `bearer` lane (no key-mode credential needed after all: record mode forwards whatever auth the caller sends): 37 requests, 37 × 200 from api.anthropic.com, 684 KB cassette
- [x] `reset-demo-repo.sh` — close open PRs (through the instance's PR routes when `--instance` is given), delete non-default branches and force-reset `main` to the pinned SHA (GitHub REST with `GITHUB_TOKEN`; the instance has no route for either); `--dry-run`; `reset-demo.test.ts` against a fake instance + GitHub API
  - [x] Run it for real against `shipit-demo-app` once — 2026-09-16, after the aborted first take: PR #2 closed through the instance's warm session (no token), then, with `GITHUB_TOKEN` from the host's root-only env file, the stale branch deleted and `main` reported already at the pin. The host has no `node`, so it runs there inside `shipit-session-worker:prod` (`docker run --rm --network host --entrypoint bash --env-file /root/shipit-demo-github.env …`, `--instance http://127.0.0.1:4123`); verified afterwards by unauthenticated reads
- [x] `host/reset-demo-instance.sh` — the plan §9 reset sequence on the host (proxy down, `stop.sh`, drop `shipit-prod_workspace`, keep credentials, `shipit_build_and_up`, proxy up), idempotent, refuses unless the project is `shipit-prod`; `--dry-run` tested against a fixture install
  - [x] Run it for real on `services` once and record whether a fresh `.shipit.db` beside the credentials volume re-runs onboarding — 2026-09-16, twice, about a minute each: no onboarding re-run; sessions and repos gone, credentials, GitHub connection and settings all intact (plan §9). One fix from the first run: the final log line said "(dry run)" on a real run too
- [x] A take never waits on a human (plan §4 item 3): storyboard `permissionMode: "auto"` verified on the composer control after the claim; every wait polls `/history` and aborts on a pending `permissionPrompt` naming the tool and path (exit 4); `driver.test.ts` covers the storyboard gate, the pure helper and `until`'s rethrow
- [x] `driver.mjs` — instance reset via `host/reset-demo-instance.sh` (workspace volume dropped, credentials kept) instead of a state dir; HTTP setup (`/api/bootstrap`, `/api/repos`, `/api/repos/trust`, `PUT /api/settings { autoCreatePr: true }`); browser at `localhost`, sidebar collapsed, `recordVideo`; beat loop with `type` / `click` actions, pane switching, `wait` conditions (`turn`, `preview_text`, `pr_card`, `merge_button`), ceiling-abort with screenshot; `beats.json`
  - [x] Phase 1 (plan §8): HTTP setup (+ storyboard `settings` / `model` pin), cursor overlay, sidebar collapse, beat loop, every wait condition implemented, ceiling-abort, `beats.json`, `selectors.mjs`; a full three-beat take against the dogfood instance
  - [x] Repo pin verified before every take (`git ls-remote` vs `repo.commit`); `--mode replay` refuses without `proxyUrl` answering `HEAD /api/hello`; `turn: finished` counts assistant groups against the send (no dependence on seeing the running state); `transcript_text` / `pr_card` / `merge_button` scoped to the transcript and the active session's card; per-beat holds recorded as footage (`beatFootageEnd`); black-splash anchor in `run.json`
  - [x] Phase 2 (2026-09-16): the reset is `host/reset-demo-instance.sh` (volumes, not a state dir), the driver runs from the session against the sslip host rather than `localhost`; `preview_text` / `pr_card` / `merge_button` / `click: merge` all held on the first complete take — after two fixes measured on the aborted first take: the beat-2 wait word (`habit`, not `streak`) and the merge-button scope (inside the card header at desktop widths; the sibling actions row is mobile-only), plus a storyboard `waitCeilingSeconds`
- [x] `make-demo-repo.sh` + `scenarios/dogfood-smoke/storyboard.json` — the phase-1 demo repo (local bare, `file://localhost/…`) and scenario
- [x] `cut.sh` — keep `[actionAt, +lead]` and `[readyAt, +hold]` per beat, concatenate, export `hero.mp4` (h264, yuv420p, faststart, no audio) and `hero.webm` (VP9, no audio); the beats are anchored on the black splash (`ffmpeg blackdetect` ↔ `run.json` `anchor.wallAt`), a take with an anchor the file does not show fails unless `CUT_UNANCHORED=1`; verified on run 9 (70.92 s → 21.000 s, exactly Σ(lead + hold))
- [x] `scenarios/website-hero/storyboard.json` — the four beats from plan §6 (plus beat 3 waiting on `merge_button: visible`, since `clickMerge` acts rather than waits), repo pin (provisional until the demo files merge), viewport, pace, `settings.autoCreatePr`, `permissionMode: "auto"`
- [x] Record the `website-hero` cassette through the settings-file redirect — 2026-09-16, second take (the first aborted on the wait word): all five beats, 37 `bearer` requests, 242.08 s recorded → 36.000 s cut = Σ(lead + hold); `cassette/` is in the branch (plan §9)
  - [x] Replay it once and confirm the fingerprint log shows no drift — 2026-09-16, replay-1 (after `reset-demo-repo.sh` moved `main` back to the pin): 37 recordings served `001`→`037` in arrival order on the `bearer` lane, 0 `cassette drift`, 0 fallback, all 200, nothing on `x-api-key`
- [x] Run the full pipeline twice from a fresh state dir and confirm the two cuts have the same length and beat boundaries (req 3) — 2026-09-16, replay-1 and replay-2, each after repo reset + `reset-demo-instance.sh` + a recreated proxy: both cuts 36.000 s with the same output-clock beat boundaries as the record cut, preview pane SSIM ≥ 0.999 at every hold start between the three, proxy logs identical (37 in order, 0 drift). Comparison table in plan §9. One on-camera variance found: the prompt's typing speed (beat 1's 120 characters took 11.0 / 6.5 / 5.9 s against a nominal 4.0 s at 30 cps), so the frame where the cut leaves beat 1's typing shows a different number of characters per take
- [x] Re-record `website-hero` with the refined storyboard (light theme, seeded streaks 4/2/0, scaffold-preview wait, Files then Preview) — 2026-09-17, take 2 after the subscription window reset: all five beats, 21 `bearer` requests all 200, the model built exactly what was asked; `cassette/` on the branch replaced with take 2 (scrubbed, no auth header)
  - [x] The anchor is the driver's own paint — the white flip before `goto` had never been in the driver (the constant sat unused), so every cut so far anchored on the instance's first paint, 0.4–1.1 s late; `clock-probe.mjs` measured Playwright's video clock a steady 0.1 s from the driver's, varying by one frame, head to tail; the flip is in, replay-3 anchored at −0.10 s (plan §4 item 6)
  - [x] A typed prompt shorter than its lead pauses before the send (`sendNotBefore`), and `cut-plan.mjs` refuses a log where it did not — take 2's record cut was 1.18 s short from that; replay-3 is 36.000 s (plan §9)
  - [x] Replay take 2 once and compare: replay-3, 21 served in order, 0 drift / fallback / unused, output-clock boundaries identical to take 1's cuts, the intended frame at every hold start (plan §9 table)
- [x] `scripts/demo-video/README.md` — one page: prerequisites on the demo host, `record` / `run` / `cut` invocations, how to add a scenario
- [x] Update plan §Key files with anything that moved during implementation; `npm run lint:dev` (does not cover `scripts/`), `npm run typecheck`
