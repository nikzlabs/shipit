.claude/skills/docs-navigator/SKILL.md:70  `issue:` resolves against the trackers declared in `shipit.yaml`. **Write the name form** (`roadmap#SHI-304`, 
docs/042-archive-disk-cleanup/plan.md:66  - **Archived workspaces** (crash-recovery backstop). `archiveSession` already drops `workspaceDir` synchronous
docs/189-inline-issue-detail/plan.md:66  identifier (`owner/repo#42` → `42`; `SHI-28` → `SHI-28`), mirroring the server's
docs/194-issue-lifecycle-workflow/plan.md:111  understands (`SHI-43`, `owner/repo#42`, or a full issue URL). Synonyms
docs/204-tracker-url-interception/plan.md:61  A bare Linear key (`SHI-28`) is intentionally **not** intercepted: it has no
docs/206-pr-card-issue-chips/plan.md:75  A bare `SHI-90` typed with no `issue` lead-in is intentionally *not* matched
docs/207-inline-issue-badges/plan.md:131  `roadmap#SHI-319`, `planning#57`, and a bare `SHI-319` in chat; confirm each
docs/248-declared-issue-trackers/plan.md:199  address (`SHI-304`, `owner/repo#42`).
docs/248-declared-issue-trackers/requirements.md:39  its issue keys carry. That is what lets a bare `SHI-304` resolve to this
docs/248-declared-issue-trackers/requirements.md:65  | the backend's canonical issue address | `SHI-304`, `owner/repo#42` |
docs/248-declared-issue-trackers/requirements.md:294  be defined there — which is what makes the bare `SHI-304` resolvable to a
src/client/components/IssuesViewer.tsx:119  * so only the bare `123` shows; Linear identifiers (`SHI-1`, no `#`) pass
src/client/components/message-markdown.tsx:37  * a bare key (`SHI-43`) or a docs/248 name form (`roadmap#SHI-319`,
src/client/components/message-markdown.tsx:333  // file-preview links; the second turns bare issue references (`SHI-43`,
src/client/stores/issues-store.ts:111  * fetch wants is the bare number after `#`. Linear identifiers (`SHI-1`, no `#`)
src/client/stores/issues-store.ts:131  /** Display identifier, e.g. "SHI-28" or "owner/repo#42". */
src/server/orchestrator/api-routes-issues.ts:413  // reference (`planning#42`, `SHI-304`, `owner/repo#42`) against exactly the
src/server/orchestrator/git-lfs-store.ts:6  * Cross-session Git LFS object sharing via the bare repo cache (docs/232, SHI-236).
src/server/orchestrator/prompts/pull-requests.md:22  - The `<pointer>` is the same reference form `shipit issue` takes. Write the declared tracker's `name#id` form
src/server/orchestrator/prompts/skeleton.md:78  Three reference forms all work: the tracker name plus the backend's id (`planning#123`, `roadmap#SHI-304`), th
src/server/orchestrator/prompts/skeleton.md:94  `issue:` points at the work item that tracks the doc, and ShipIt renders a jump-to-issue chip from it. Write i
src/server/orchestrator/trackers/linear/adapter.ts:148  * name, the bare `SHI-304` otherwise. Threaded in rather than applied at the
src/server/session/agent-shim/shipit-issue.ts:26  *    (`planning#42`, `roadmap#SHI-304`, `owner/repo#42`, `SHI-304`) or with
src/server/session/agent-shim/shipit-issue.ts:97  * bare `42` or `SHI-9` with no declaration to match. Passing both is only
src/server/session/agent-shim/shipit-issue.ts:112  `shipit issue ${verb}: a pointer is required (e.g. planning#42, SHI-28, owner/repo#42, or an issue URL).`,
src/server/session/agent-shim/shipit-issue.ts:159  * Interpret a bare id (`42`, `SHI-9`) against an explicitly named destination.
src/server/session/agent-shim/shipit-issue.ts:248  * Provenance label for the untrusted-input envelope, e.g. `linear:SHI-28` or
src/server/session/agent-shim/shipit-issue.ts:335  // declarations too (`roadmap#SHI-204` and a bare `SHI-204` both work). It must
src/server/session/agent-shim/shipit.ts:155  all work: 'planning#42' / 'roadmap#SHI-304' (name + backend id), 'roadmap#304'
src/server/session/agent-shim/shipit.ts:156  (name + number), and the backend's own address ('SHI-304', 'owner/repo#42', an
src/server/shared/declared-tracker.ts:37  * what lets a bare `SHI-304` resolve to this declaration. The *workspace* comes
src/server/shared/pr-issue-refs.ts:22  * `roadmap#SHI-304`) or the backend's own address (`SHI-43`, `owner/repo#42`, a
src/server/shared/shipit-config.ts:485  // its issue keys carry. That is what lets a bare `SHI-304` resolve to this
src/server/shared/types/domain-types/issue.ts:285  /** Display identifier, e.g. "SHI-28" or "owner/repo#42". */
src/server/shared/types/domain-types/issue.ts:343  /** Display identifier, e.g. "SHI-28" or "owner/repo#42". */
src/server/shared/types/domain-types/issue.ts:458  /** Human-facing identifier, e.g. "SHI-67" or "owner/repo#123". */
src/server/shipit-docs/design-docs.md:51  | **Preferred** — the declared tracker's name plus the backend id | `planning#42`, `roadmap#SHI-304` |
src/server/shipit-docs/issues.md:42  | the backend's canonical address | `SHI-304`, `owner/repo#42`, an issue URL |
src/server/shipit-docs/issues.md:46  the `name#id` form (`planning#42`, `roadmap#SHI-304`). It survives the
src/server/shipit-docs/issues.md:383  - `<reference>` is the tracker-neutral form above (`SHI-43`, `owner/repo#42`,
src/server/shipit-docs/shipit-yaml.md:261  | `team` | string | `linear` | The Linear team key (`SHI`) — also the prefix its issue keys carry, which is wh
src/server/shipit-docs/shipit-yaml.md:283  | the backend's canonical address | `SHI-304`, `owner/repo#42`, an issue URL |
