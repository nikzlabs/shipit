The rebase onto `{{BASE_BRANCH}}` you resolved conflicts for has concluded{{SHA_SUFFIX}}.{{PUSH_SENTENCE}}

Your working tree was rewritten from outside the session: files you read earlier in this conversation may have changed, and commit SHAs you noted are stale. Re-read any file before editing it rather than relying on an earlier read, and do not try to undo the rebase or re-apply anything it brought in.

You asked to continue once the rebase concluded, with this note:

{{NOTES}}

Do that now. If it no longer applies — the user has since redirected you, or the work is already done — say so instead.
