<?xml version="1.0" encoding="utf-8"?>
<!-- Faint translucent circle so the floating settings button stays tappable
     over arbitrary page content without drawing a heavy opaque chip. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#330F172A" />
</shape>
