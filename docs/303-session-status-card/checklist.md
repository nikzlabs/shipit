# Session status card — checklist

Implementation of [plan.md](plan.md); the design deliverables shipped in the
docs-only PR and their review history is on planning#550.

- [x] `advanced.sessionStatusCard` in the settings catalogue, off by default; save hook marks stored cards stale on false → true.
- [x] Flag on the per-turn run params → `SHIPIT_SESSION_STATUS_CARD` in the spawn env, `writeMcpConfig` context, the five adapter tool lists, Claude's allowlists; resident reuse check against the flag.
- [x] `session_status` tool and its bridge registry entry; `validateActionItems` extracted and shared; envelope validation with every field optional (a bare call confirms); worker relay.
- [x] Orchestrator route: validate, merge the delta, provenance, reconcile offers, persist, broadcast, `statusUpdated`, reply with the offered list; refuse a bare call with no stored card. `propose_actions` route refuses under the flag.
- [x] `sessions.session_status` column and `SessionInfo.sessionStatus`; `recordSessionStatus`, `markSessionStatusStale(ifWriteSeq)`, `takeOfferedActions`, `runStatusExclusive`.
- [x] `statusUpdated` on `TurnAccumulator`.
- [x] `settleTurnFacts` on all four terminal paths before the drain, with the immediate guarded stale mark; reset on adoption; memoized decision after idle; dispatch from `finishTurn` via the drain entry.
- [x] `statusNudge` and `silent` through `AgentDispatchInit`, `QueuedMessage`, `toQueuedMessage`, `queuedMessageToDispatchOptions`, `TurnInput`.
- [x] Lifecycle: stale on rewind/reset; copy-as-stale on fork.
- [x] `checklistAccepted` takes offers by `offerId` after admission; busy-path ordering fix.
- [x] Split `ActionChecklistCard` into the shared checklist and two wrappers; per-offer provenance in the status card's submit message.
- [x] `SessionStatusCard` as the last child of the message list's content element, scrolling with the conversation: two fields, offers with one Submit, taken offers greyed, tagged SENT and still tickable, "Stale" label bottom-right.
- [x] Prompt: two variants at module load; the flag-on section, in the injected system prompt, replaces the follow-up-actions section, keeps its §5 boundary and says to call the tool bare when nothing changed; composition tests per variant.
- [x] Tests listed in plan.md, including the flag-off byte-for-byte checks per harness.
- [x] Req 30: the card keeps its turn-start place while a turn runs and returns to the end when it stops — frozen anchor, one keyed list so the move is not a remount, a group keyed by the chunk it belongs to, and the reading anchor restored under the card's own scroll guard.
- [x] Verify req 30 by hand in the dogfood instance on a narrow viewport: a seeded card, a real turn, the card scrolling away and coming back.
- [x] Req 31: `lastTurn` through the domain type, the validator, the service merge as the one non-delta field, the row parsing, the tool schema, the route reply, the injected prompt and the nudge prompt; the card renders it first, labelled beside a labelled status, and hides it when stale.
- [x] Req 32: `pending-answer.ts`; every answer card in a container keyed by its tool, the pending one last in the flow under the status card; the chunk piece counter that replaces `openedByCard`, with the chunk tracked rather than recomputed.
- [x] Verify both by hand in the dogfood instance at 390x780: a turn ending with a question, with a card and offers present, and a card whose last-turn line changes.
- [x] Verify in the dogfood instance: flag on — switch away and back, reload, a turn that skips the tool, a resident agent across a toggle; flag off — an action card as today.
- [x] Req 33: three capped, accent-tinted cards — Status, Next steps, Last turn — with the Stale mark in the status cap; the unticked checkbox given a surface of its own so it survives the tint; `mockup.html` redrawn as the shipped look, with every drawn round kept beside it; reqs 14, 28 and 31 amended.
- [x] Verify req 33 by hand in the dogfood instance, wide and at 390x780, fresh and stale, in a light and a dark theme.
- [x] Req 33, second pass: two tones — soft caps on Status and Last turn, the filled cap on Next steps, which moves last; the Stale mark redrawn in the accent for the now-soft status cap.
- [x] Verify the two tones by hand in the dogfood instance, including the empty case: with no manual step and no offer the whole "Next steps" card is absent, leaving the two soft cards.
- [x] Req 33, third pass: a third tone — Last turn leaves the accent for the ordinary card surface, after an inventory showed `--color-info` equals `--color-accent` in three themes and `--color-pr` means "pull request"; `look-lastturn.html` keeps the four candidates.
- [x] Req 35 (planning#591): the stored card rides every turn's prompt — `formatSessionStatusContext` and its 8000-char cap, `sessionStatusTurnContext` behind the setting, the prefix in both composition sites, `SystemTurnDeps.sessionStatusContext`; the nudge prompt rewritten to carry the same block and ask for a line-by-line reconciliation; the injected section told to reconcile against the block rather than from memory.
- [x] Req 34 (planning#589): a turn a message reached after it started is not nudged — `steered` and `promptQueued`, both snapshotted in `settleTurnFacts`, join `running` and the queue as pending successors, so a message is never preempted by a system turn that retires the process holding it.
- [x] Req 37: five options drawn in `look-step-comment.html` and ruled on; a note per manual step behind a control on the row, sendable without ticking; `renderTrailing`/`renderBelow` on the shared checklist so the control sits outside the label; `ChecklistItem.tag` for ANSWERED; the done/answered split and the per-step `Note:` line in the submit message; the injected prompt and the tool description told what comes back.
- [x] Verify req 37 by hand in the dogfood instance, wide and at 390x780, in a light and a dark theme: open a note, answer a step without ticking it, submit, and read the message the agent received. This is what caught a ticked step wrongly reading ANSWERED, which its own test had asserted after the submit that cleared it.
- [x] Review findings folded in: the note control is a toggle and nothing closes the field on blur (a blur close swallowed the click that caused it); the message and "Add comment…" both fold a step's text onto one line and indent every line of a note; a whitespace note marks and sends nothing; a new note on a sent row reads ANSWERED beside SENT; the two weak guards replaced.
- [x] Req 36 (planning#594): the settlement asks what the turn DID, not who started it — `harnessCommand` replaces the `silent` exemption in both the nudge and the stale mark, so a compaction the user asks for is exempt alongside the one ShipIt starts; scoped to the turn being settled so a CLI-started successor cannot inherit it, and withheld when the command arrives wrapped as another session's message. Every other ShipIt-started turn stays checked, each walked against the rule and tabulated in plan.md.
- [x] planning#595: opening a session lands at the end of its conversation — the follow-the-bottom and gesture refs in `useMessageScroll` reset on the displayed session, because the card is the whole of the content in the loading gap and removes the clamp the open path was relying on for its repair; guarded on the real list through the whole open sequence.
- [x] Req 38: the ask is a line in the next turn's prompt, not a turn — `nudgePending` on the card, `settleSessionStatusCard` as the settling turn's one write, `shouldCarryStatusNudge` down to the four exemptions that stand on their own terms, `awaitingUserAnswer` splitting req 13's question from a user stop, a session with no card asked for its first, and the whole `statusNudge` dispatch/worker/adoption plumbing deleted with the turn it identified.
- [x] Req 39: the reconciliation instruction closes the block instead of opening it, in `prompts/status-card-reconcile.md`, with the miss notice beside it.
- [x] Req 40: `turnSeq` on the card as the clock, `offeredSeq`/`takenSeq`/`stepSeq` on the entries, printed as "offered 9 turns ago"; an entry stored earlier says it has no recorded turn.
- [x] Review findings folded in: an exempt turn no longer drops an outstanding ask; a stop the harness answers by exiting rather than by a result is not read as a crash (`userStopped`, and the test staged the way a stop really ends); a legacy manual step keeps its unrecorded age through a bare confirmation (`stepSeq` holds `null`); the driver-owned turn stops being a fifth exemption; and the drain awaits the card write so the ask reaches the very next prompt.
- [x] Establish from the code which gate swallowed the drift report's text-only turns: four shapes, each reproduced against `main` in a throwaway clone — a plain text-only turn IS nudged there, so no gate reads tool use.
- [x] Req 41: `InlineMarkdown` — the same pipeline without the prose block, so a one-line field keeps its line box — behind the checklist rows' label and description and the single-action card's two lines; ShipIt pointers enabled there because every row comes from an agent tool call; the tool description and the injected prompt told the fields are markdown.
- [x] planning#595, third report: a scroll made during the loading gap is discarded when the rows first render, so a reader who scrolled a tall card still opens at the end — the card has no scroll container of its own, measured, so "the gesture landed in the card" cannot be the signal; reproduced in Chrome before the change (PR #2942).
- [x] Req 42: collapse the pinned card to an icon — manual only, sticky for the session in `localStorage` keyed by session id, never folds itself up; a caret at the bottom-right of whichever card is *last* (the stack's shape moves), and a right-aligned icon carrying the outstanding counts and the Stale mark to restore it. `scripts/seed-inner-status-card.ts` seeds a card into the dogfood instance so the stack can be looked at without spending a turn (PR #2945).
- [x] Review findings folded in: a row counts as outstanding while anything about it is *unsent*, not merely `!taken`, so a re-ticked offer and a new note on a reported step are not hidden by collapsing; the checklist's per-text sent/note state scoped to an `owner` session, after two sessions with an identically worded step shared a SENT grey; that guard held in state rather than a ref, which survives a discarded render; focus handed to the control that replaced the one pressed, and a note field focused by the press that opens it rather than by `autoFocus`; and the collapsed form reduced to the single icon req 42 asks for, its words moved to the accessible name.
- [x] Req 43: the stack sits immediately above the composer while the conversation is shorter than the scroller, and does not jump when the rows land — a flex-column scroller with `mt-auto` on the content element, chosen over `justify-end` (unreachable overflow) and over measuring-and-padding (a correction that lands after paint, which is the jump itself). Verified by hand in the dogfood instance, wide and at 390x780: short, empty, the loading gap instrumented per frame, a long conversation, and a real running turn for req 30; the loading-gap scroll is still discarded (PR #2942). Review findings folded in: req 43 names what stays below the card (reqs 30 and 32), so its "whenever" cannot be read against them; and the plan no longer claims a second ResizeObserver or a scroll range that short content never had.
- [x] Review findings folded in: the transcript card's own title renders as markdown too; the "never from ingested content" claim softened to what is true (the agent composed it, at the trust level of its own prose); the row's markdown moved back out of the `<label>` after Chromium showed an href-less anchor ticks the checkbox and jsdom cannot; a test that the rows opted into ShipIt pointers, without which removing `shipitLinks` stayed green.
- [x] Req 44: a row that was ticked when it was sent keeps a tick in its box — `ChecklistItem.takenChecked` drawn as a muted box beside the accent one the selection uses, so Submit still sends only what is selected and a re-tick hides the record under it; every sent offer carries it, and manual steps get a `checkedSteps` set in which each send rewrites the rows it submitted and leaves the rest, so a step later answered with a note alone loses its tick while untouched rows keep theirs.
