# Checklist — Re-arm a merged session for a new PR after rebase

- [x] `git.ts`: add two-dot diff helper + `advancedBeyondMergedBase(base)` (merge-base == base tip AND non-empty two-dot diff)
- [x] `sessions.ts`: add `clearMerged(id)` (sets `merged_at = NULL`, stashes prior-PR breadcrumb) + breadcrumb column/migration/`toRow`/`fromRow`
- [x] `pr-status-poller.ts`: add `reArm(sessionId, supersededPrNumber)` — **silently** clear `lastKnown` + `mergedSessions` + `setPrStatus(null)` (NO removal broadcast — it would race the WS card across transports), record superseded PR number, then `trackSession`
- [x] `pr-status-poller.ts`: `verifyMissingPr` ignores a terminal (merged/closed) result whose `number` == the recorded superseded number, until a different-numbered PR appears (else the immediate `trackSession` poll re-promotes the OLD merged PR via `findPullRequestAnyState` and undoes the re-arm in one tick)
- [x] Re-arm orchestration in a **shared helper** called by BOTH `postTurnPrFlow` sites (`ws-handlers/agent-execution.ts` AND `runner-registry-factory.ts` dispatch/system-turn): detect → `clearMerged` → `reArm` → `sseBroadcast("session_list")` → then card emit. (Don't put it in `emitPrLifecycleAfterCommit` — `PrLifecycleDeps` has only the per-connection WS `emit`, no `sseBroadcast`.)
- [x] `services/pr-lifecycle.ts`: detection helper + thread `previousMergedPr` breadcrumb onto the `ready`/`open` card; relax the `if (session.mergedAt) return` guard for the progressed case
- [x] `pr-store.ts`: amend `updateCard` terminal-regress guard (311-314) so a card carrying `previousMergedPr` replaces a `merged`/`closed` card (order-independent override)
- [x] Breadcrumb carries `baseBranch`; re-arm targets the prior PR's base for the new PR (ready diff + `quickCreatePr` base), not auto-detected `main` (`pr-lifecycle.ts` ~161, `github.ts` ~308-312)
- [x] Re-armed PR push uses `--force-with-lease` (gated on re-arm state) so a surviving, diverged remote branch (auto-delete off / best-effort delete failed) doesn't reject the push (`github.ts` ~297)
- [x] `session_list` SSE rebroadcast after `clearMerged` so the sidebar regroups to Active/gray live, not only on reload (consumed client-side over SSE only)
- [x] Test: re-armed `ready` card carrying `previousMergedPr` is accepted over a stale merged card (regression test against the `updateCard` guard); re-arm does not emit a destructive `pr_status` removal
- [x] Test: after re-arm, a poll where `findPullRequestAnyState` returns the superseded merged PR does NOT re-promote (suppression holds); a new different-numbered PR clears the suppression and tracks normally
- [x] Test: rebase detected in a dispatch/system turn (not just interactive) re-arms (covers both `postTurnPrFlow` sites) — shared helper unit-tested; WS site covered end-to-end in `pr-auto-create-on-turn.test.ts`; the dispatch site calls the identical helper
- [x] Test: re-armed session whose merged PR targeted a non-`main` base opens the new PR against that same base
- [x] Test: re-armed PR creation succeeds when the old remote branch still exists and has diverged (force-with-lease path)
- [x] Note in design: secondary-viewer (auto-create OFF) keeps stale merged card until reconnect/new-PR — accepted edge
- [x] Requirement: merged-but-progressed session is never visually archived and never fast-evicted (driven by `clearMerged` clearing `merged_at`; asserted in `sessions.test.ts`)
- [x] Tests: git detection matrix (squash/regular × rebased/not × work/clean)
- [x] Tests: `clearMerged` unit; poller re-arm integration; post-turn merged+progressed → new PR vs merged+clean → stays merged
- [x] Resolve open question: re-armed card wording → "previously merged" breadcrumb, gray fresh-session indicator
- [x] `PrLifecycleCard.tsx`: render "Previously merged #N" note on the re-armed `ready` card
- [x] Confirm sidebar row leaves "Recently resolved" → Active with gray (no merge icon) after re-arm (natural result of cleared `merged_at`; `clearMerged` test asserts the session returns to the visible Active set)
- [x] Update this plan with any subsystems/patterns discovered during implementation
