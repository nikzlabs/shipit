Your PR #{{PR_NUMBER}}{{PR_TITLE_SUFFIX}} merged.{{BRANCH_LINE}}

Run `shipit branch reset-to-base` before editing. If it fails, stop, do not reset manually, and report the error. Then continue the earlier request without recreating merged work. If another PR must merge before more work, re-arm with `shipit session notify-on-merge --self`.
