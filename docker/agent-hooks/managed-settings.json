{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "ShipIt-managed Claude Code settings. Baked into /etc/shipit/managed-settings.json in the session-worker image. The orchestrator passes --settings to the Claude CLI only when autoCreatePr is on — see src/server/session/claude.ts and docs/129-stop-hook-pr-enforcement/plan.md.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/etc/shipit/agent-hooks/stop-pr-check.sh"
          }
        ]
      }
    ]
  }
}
