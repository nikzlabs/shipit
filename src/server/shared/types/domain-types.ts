// Facade: domain-types.ts was split by domain into ./domain-types/*.ts
// (docs/201 Phase P2). It remains the public entry point so the export surface
// is byte-identical and no import site changes. See ./domain-types/index.ts.
export * from "./domain-types/index.js";
