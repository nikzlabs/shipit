ARG BASE_IMAGE=shipit-session-worker:dev
FROM ${BASE_IMAGE}

RUN curl -fsSL https://download.docker.com/linux/static/stable/$(uname -m)/docker-27.5.1.tgz \
    | tar xz --strip-components=1 -C /usr/local/bin docker/docker

RUN mkdir -p /usr/local/lib/docker/cli-plugins && \
    ARCH=$(uname -m) && \
    curl -fsSL "https://github.com/docker/compose/releases/download/v2.32.4/docker-compose-linux-${ARCH}" \
    -o /usr/local/lib/docker/cli-plugins/docker-compose && \
    chmod +x /usr/local/lib/docker/cli-plugins/docker-compose

RUN apt-get update && apt-get install -y --no-install-recommends \
    systemd \
    && rm -rf /var/lib/apt/lists/*

# The entrypoint also aligns the host journal GID and preserves supplementary groups.
RUN groupadd -rf systemd-journal \
 && groupadd -rf adm \
 && usermod -aG systemd-journal shipit \
 && usermod -aG adm shipit \
 && id -nG shipit | tr ' ' '\n' | grep -qx systemd-journal \
 && id -nG shipit | tr ' ' '\n' | grep -qx adm
